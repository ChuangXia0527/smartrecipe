<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 相机权限 -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- 语音识别录音权限 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- （可选）相册读取：Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- （可选）Android 12及以下读取相册 -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Smartrecipe"
        tools:targetApi="31"
        tools:replace="android:theme,android:label">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 拍照识别页 -->
        <activity android:name=".ui.recognize.RecognizeActivity" />

        <!-- 拍照识别结果/勾选页 -->
        <activity android:name=".ui.recognize.RecognizeResultActivity" />

        <!-- 详情页 -->
        <activity android:name=".ui.detail.RecipeDetailActivity" />

        <!-- 语音页（如果你还保留） -->
        <activity android:name=".ui.voice.VoiceActivity" />

    </application>

</manifest>
